# API Configuration
# Base URL for the backend API

# Development: Leave empty (uses Vite proxy to avoid CORS)
VITE_API_BASE_URL=

# Production: Set full URL
# VITE_API_BASE_URL=https://api.seismic-events.com
